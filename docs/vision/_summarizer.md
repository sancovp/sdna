# vision: Slinky (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:36:00  FINDING: CORRECTION (I called slinky 'dead' earlier — WRONG; I had not read it, now I have, slinky_v2.py in full). slinky v2 IS a complete, runnable transcript-compaction ENGINE: identify_iterations (parse CC .jsonl into user-text-bounded iterations), compress_iteration + replace_content (swap message content for [box ref] refs preserving structure), slinky_compress (load->compress all-but-last-N->write->real compression-ratio stats), with a __main__. The ONLY stub is mock_summary() (slinky_v2.py:93-108) = a canned-string placeholder EXACTLY where a real summarizer plugs in. So slinky is BUILT-BUT-UNWIRED at the summarizer seam, NOT dead. The summarizer is a SEPARATE module (Isaac: 'the summarizer is a different module, it isnt hooked up') = the hierarchical-summarizer we already have; wiring it into mock_summary's slot completes slinky. (There are 4 slinky files: slinky_context.py 451, slinky_full.py 373, slinky_manager.py 667, slinky_v2.py 350 — only v2 read so far; the other 3 = variants, not yet read by me.) ISAAC'S REAL POINT = unify CONTEXT-ENGINEERING OPERATIONS across a HEAVEN HISTORY and a CLAUDE TRANSCRIPT (one set of ops over both substrates). Current ContextEngineering (context_engineering.py 1306) source-abstraction = ParsedMessage 'from tmux or SDK transcript' (line 60); reads tmux pane (parse_tmux_messages) OR an SDK transcript FILE (_get_sdk_messages, line 1102) — but that reader points at /tmp/sdna_transcripts/{id}.json which NOTHING writes (dead path). NO heaven-history source in CE (grep heaven/get_history EMPTY). So unification = a common ConversationSource interface with 2 adapters (HeavenHistoryAdapter reading heaven history; ClaudeTranscriptAdapter reading ~/.claude/projects/<slug>/<sid>.jsonl) feeding the same ParsedMessage stream, so weave/inject/dovetail/slinky-compress operate identically on both. TIE-IN: the runner.py:138 fix (thread the real claude transcript_path out of SDNA) also lets CE's _get_sdk_messages be repointed at the REAL transcript instead of the dead /tmp path. SLINKY TESTING = do it REMOTELY on a container with a claude-code token (Isaac: 'kiki's container / jobworld'), NOT this container — slinky rewrites .jsonl in place and must not touch this live session's transcripts; needs a research pass there. (CE lib core read via grep + _get_sdk_messages; full 1306-line read not yet done by me.)  tags:[Slinky, Context_Engineering, Transcripts, Heaven, Summarizer, Correction]
