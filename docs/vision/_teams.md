# vision: Sdna (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:30:09  FINDING: 4-AGENT TEST TEAM RESULTS (load-bearing claims re-verified by me). (1) INTERFACE FRICTION = CONFIRMED + the fix is ONE site: claude path runner.agent_step returns output={'text':final_text} ONLY (runner.py:138), stranding session_id (captured runner.py:110-113) + raw messages; heaven returns an 8-key dict (history_id/agent_status/extracted_content...) at heaven_runner.py:258-267 -> a heaven-written caller gets only ctx['text'] from claude = the parity pain. His preferred 'pass dict DOWN, no parity' = the DOWN leg ALREADY EXISTS (config.to_sdk_options unpacks flat config -> ClaudeAgentOptions, config.py:224-257); the UP leg is the gap = make runner.py:138 build a flat normalized dict {text,session_id,transcript_path,raw} mirroring heaven's shape so SDNAC ctx.update carries it. PROPOSED SEAM (read runner.py in full before editing). (2) TRANSCRIPTS = his suspicion CORRECT: the CLI writes a real parseable .jsonl keyed by session_id under ~/.claude/projects/<slug>/ (confirmed by an E2E run), but SDNA never threads session_id forward + its 2 utils are dead (_get_sdk_messages reads /tmp/sdna_transcripts/{id}.json which NOTHING writes -> always []; find_current_transcript = tmux/slinky tool, hardcoded -home-GOD). promptworld ALREADY computes the correct transcript path (p_main_agent.py:148-158) — SDNA hasn't. Same runner.py:138 fix threads it. (3) CRON: SDNA cron.py = base (CronJob/CronScheduler timing-only, pass executor); CAVE automation.py = Automation(Link) on top, imports ONLY sdna (grep flask/http/db EMPTY, VERIFIED), imports + FIRES headless (I confirmed /tmp/cron_fire_proof.json). GAP = no session-surviving daemon (CAVE driver = CAVEAgent Heart, needs agent). Lightest off-session path = OS cron/systemd-timer -> ~10-line runner importing existing primitives, executor=subprocess claude -p. NO new system. (4) TEAMS: promptworld = NO team (one claude -p per alias, by design; 'team' dept unimplemented). claude -p EXPOSES full team toolset (I VERIFIED LIVE: Task/TeamCreate/TeamDelete/TaskCreate/SendMessage/Monitor present, runs OK w/ --model sonnet); promptworld passes no --allowed-tools so a turn INHERITS them -> promptworld can go multi-agent nearly FREE. Residual: full nested-team E2E inside -p not yet run. CAVE 'teams' = NOT claude TeamCreate, it's the llegos Actor inbox/channel fabric (cave_agents/agent_registry/message_router/remote_agents), remote arm needs SDNA+Heaven + FastAPI server, live-working UNKNOWN/weak evidence = heavyweight legacy path. (5) CHAIN/COMPILER+LIBS: chain_ontology real (Link/Chain/EvalChain/Compiler, zero heaven dep) BUT Compiler has no real compile method + NOT in __all__ (verified). slinky = DEAD (only self-referential caller, mock summaries no LLM) = ignore/rebuild-later-with-real-summarizer. ContextEngineering = partially LIVE, heaven-free, dual-transport incl SDKTransport->claude_agent_sdk.query, used by cave/core/dna.py + Ariadne (one broken treeshell caller) = the MOST reusable surface for a Compoctopus-on-Claude-Code-SDK + the 'skills for programming SDNA -> SOPHIA' idea. NET: the parity pain = 1 return site; transcripts ride the same fix; cron = OS-timer over existing headless primitives; promptworld-teams = free via claude -p native; ContextEngineering = the SDK-native compile substrate; slinky dead. OPEN: 'nomicon system' scope not yet grounded (need to define/locate before building).  tags:[Sdna, Claude_Code_Sdk, Promptworld, Teams, Cron, Transcripts, Context_Engineering, Slinky]
