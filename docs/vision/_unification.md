# vision: Sdna (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:15:03  FINDING: KEYSTONE (verified by me reading the code, not the agent report): SDNA ALREADY programs the Claude Code SDK as its DEFAULT backend. config.py:110 backend default = 'claude'; poimandres.py:55-64 routes default to runner.agent_step which drives claude_agent_sdk.query (runner.py:13 import, runner.py:99 call); config.to_sdk_options (config.py:224-257) maps the full ClaudeAgentOptions surface (resume/fork/session_id/mcp/hooks/agents/plugins/model). HEAVEN is now the OPTIONAL branch (backend=='heaven'). So 'fix SDNA to use the claude code SDK' is structurally DONE — not a rewrite. REMAINING (honest gaps, UNVERIFIED): (1) parity plumbing — claude path returns only text+session_id while heaven returns history_id/agent_status/extracted_content used for cross-SDNAC continuation; wiring SDK session_id (captured runner.py:110-113) into ctx as the continuation key is the missing piece, seam = runner.py:88-139; (2) E2E UNVERIFIED that real multi-step flows' dovetail/OVP logic runs on the leaner claude output; (3) container UNKNOWN whether claude_agent_sdk is installed/authed in deploy containers (guarded import -> ERROR if absent); (4) minor incoherence: code WARNS 'using Claude SDK backend, set SDNA_ALLOW_CLAUDE_SDK=1' as if heaven were canonical, yet 'claude' is the declared default. SEPARATELY: promptworld runs on claude -p CLI subprocess (p_main_agent.py:131-183, --resume/--session-id, scrubs ANTHROPIC_API_KEY to bill subscription), ZERO sdna on its agent path, works E2E; finishing it = CEO-persona inject into /api/chat (v2.1) + the other 6 of 7 departments (by-design deferred). promptworld is INDEPENDENT of the SDNA question.  tags:[Sdna, Claude_Code_Sdk, Promptworld, Unification]
